<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @org.name %>
</p>

<p>
  <b>Desc:</b>
  <%= @org.desc %>
</p>

<p>
  <b>Members:</b>
  <%= @org.members %>
</p>

<% if @current_user.present? && (@current_user.admin? || @org.members.include?(@current_user))%><%= link_to 'Edit', edit_org_path(@org) %> | <% end %>
<%= link_to 'Back', orgs_path %>
