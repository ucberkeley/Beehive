Welcome, <%= current_user.name %>!
<br>
<br><br>
Quick Links: 
<br>View Listings
<% form_for :job, :url => { :controller => "jobs", :action => "list" } do |f| %>
				<%= select_tag(:department_select, "<option disabled>--UC Berkeley--</option><option value='0'>All Departments</option>" + options_from_collection_for_select(@departments, :id, :name),  { :onchange => "this.form.submit();"}) %>
			<% end %>
<br> <%= link_to "Post a job", :controller => "jobs", :action => 'new'%>
<br> <%= link_to "Browse Reviews", :controller => "jobs", :action => 'new'%>
